import { extendTheme } from '@chakra-ui/react'
import '@fontsource/dm-serif-display'

const theme = extendTheme({
  fonts: {
    heading: `'DM Serif Display', dm-serif-display`,
    body: `'DM Serif Display', dm-serif-display`
  }
})

export default theme
